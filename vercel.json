{
  "buildCommand": "npm run build",
  "outputDirectory": "dist/control-escolar-webapp",
  "framework": "angular",
  "ignoreCommand": "echo 'Use Vercel settings for root directory'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
